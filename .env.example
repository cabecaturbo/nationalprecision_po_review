# Anthropic API key — used ONLY by the server-side routes:
#   /app/api/extract/route.ts   (PDF field extraction)
#   /app/api/clauses/route.ts   (quality-clause summary, web search enabled)
# Get a key at https://console.anthropic.com/ .
# Copy this file to .env.local and fill in the value. Never commit real keys.
ANTHROPIC_API_KEY=
